{
  "name": "last-stand",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "jsdom": "^24.0.0",
    "@vitest/coverage-v8": "^1.2.0"
  }
}